<!DOCTYPE html>
<html>
  <head>
    <title>食べたレコード</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%= favicon_link_tag('eat-icon.png') %>
    <%= auto_discovery_link_tag(:json, {controller: "/eat_records", action: "manifest"}, {rel: "manifest", href: manifest_eat_records_path }) %>
  </head>

  <body>
    <div class="container">
      <nav class="navbar navbar-expand-lg navbar-light" style="background-color: #e3f2fd;">
        <div class="container-fluid">
          <%= link_to "EatRecords", eat_records_path, class: "navbar-brand" %>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
              <li class="nav-item">
                <%= link_to "説明", desc_eat_records_path, class: "nav-link active" %>
              </li>
              <!-- li class="nav-item">
                <a class="nav-link active" aria-current="page" href="">ドメインのトップ</a>
              </li -->
            </ul>
          </div>
        </div>
      </nav>

      <h1>食べたレコード</h1>
      <hr>
      <%= yield %>
      <hr>
      <p class="notice"><%= notice %></p>
      <p class="alert"><%= alert %></p>
      <footer class="footer mt-auto py-3 bg-light">
        <div class="container">
          <span class="text-muted"></span>
        </div>
      </footer>
    </div>
  </body>
</html>
